# Copy this file to .env and fill in all three values. Without them the app falls
# back to an offline text-similarity heuristic and never calls the LLM.
# See README.md "Choosing a free provider" for provider-specific values.

LLM_API_KEY=
LLM_BASE_URL=
LLM_MODEL=
