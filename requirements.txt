gradio==6.26.0
pymupdf4llm==1.27.2.3
openai==2.41.1
pydantic==2.13.4
python-dotenv==1.2.2
pytest==9.1.0

# OCR fallback — only needed for SCANNED/image PDFs (imported lazily, so the app
# and the digital-PDF/.txt paths run fine without it). Pulls in onnxruntime and the
# OCR models, which dominate install size and cold-start time.
rapidocr-onnxruntime==1.2.3
